services:
  - type: web
    name: techstore-soap
    runtime: python
    rootDir: soap-facturacion
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        sync: false
